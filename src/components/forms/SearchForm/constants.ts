import { FormInitialValues } from "./types";

export const BTN_POSITION = "flex justify-end";
export const BTN_STYLE =
  "disabled:opacity-50 py-2 px-5 mt-2 mb-1 bg-primary text-white text-base font-headline font-bold rounded-xl hover:bg-primary-hover focus:outline-none focus:ring-2 focus:ring-primary focus:ring-opacity-40 transition-colors";

export const initialValues: FormInitialValues = {
  movieQuery: "",
};